pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

if (!System.env.JITPACK) {
    include ':FtcRobotController'
    include ':TeamCode'
}
include ':PracticeApp'